---
phase: 07-hierarchical-agents
plan: 04
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreProperties.kt
  - kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreAgentFactory.kt
  - kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreAutoConfiguration.kt
  - kore-spring/src/test/kotlin/io/github/unityinflow/kore/spring/KoreHierarchyPropertiesTest.kt
autonomous: true
requirements: [HIER-03]
must_haves:
  truths:
    - "kore.hierarchy.max-depth is bindable (@ConfigurationProperties), defaults to 5, and is threaded into every agent built via KoreAgentFactory (D-08)."
    - "With no kore.hierarchy.* config, agents behave as before (default maxDepth 5) — binary/behavioral compatibility preserved (criterion #5)."
    - "A user maxDepth(n) override in the agent { } block still wins over the Spring default."
  artifacts:
    - path: "kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreProperties.kt"
      provides: "hierarchy: HierarchyProperties = HierarchyProperties() + nested data class HierarchyProperties(maxDepth: Int = 5)"
      contains: "HierarchyProperties"
    - path: "kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreAgentFactory.kt"
      provides: "factory applies maxDepth(maxDepth) before block so the property default flows into the loop"
      contains: "maxDepth"
    - path: "kore-spring/src/test/kotlin/io/github/unityinflow/kore/spring/KoreHierarchyPropertiesTest.kt"
      provides: "ApplicationContextRunner test: default 5, override binds, factory threads it"
      contains: "KoreHierarchyPropertiesTest"
  key_links:
    - from: "KoreProperties.hierarchy.maxDepth"
      to: "AgentBuilder.maxDepth(n)"
      via: "KoreAgentFactory.agent applies maxDepth(properties.hierarchy.maxDepth) before block()"
      pattern: "maxDepth\\("
---

<objective>
Surface the depth ceiling through Spring config (D-08). Add a
`KoreProperties.hierarchy.maxDepth` property (default 5, mirroring the existing
`budget.defaultMaxTokens` template), and thread it into agents built by
`KoreAgentFactory` by calling `maxDepth(properties.hierarchy.maxDepth)` before the
user's `block()` (so a user `maxDepth(n)` in the agent block still wins). Add a
Spring test proving the default, the override binding, and that the factory threads
the value.

Depends on Plan 03 for the DSL method `AgentBuilder.maxDepth(n)`. This is the only
plan touching kore-spring (wave 3, after Plan 03).
Output: modified `KoreProperties.kt`, `KoreAgentFactory.kt`,
`KoreAutoConfiguration.kt` (factory bean now takes KoreProperties), new
`KoreHierarchyPropertiesTest.kt`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hierarchical-agents/07-CONTEXT.md
@.planning/phases/07-hierarchical-agents/07-RESEARCH.md
@.planning/phases/07-hierarchical-agents/07-PATTERNS.md
@kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreProperties.kt
@kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreAgentFactory.kt
@kore-spring/src/main/kotlin/io/github/unityinflow/kore/spring/KoreAutoConfiguration.kt
</context>

<artifacts_this_phase_produces>
- `KoreProperties.HierarchyProperties(val maxDepth: Int = 5)` + `hierarchy` field on root data class (D-08).
- `KoreAgentFactory` constructor gains the maxDepth value (or KoreProperties) and applies `maxDepth(...)` before block.
- `KoreAutoConfiguration.koreAgentFactory(...)` bean passes `properties.hierarchy.maxDepth`.
- `KoreHierarchyPropertiesTest` (NEW) — ApplicationContextRunner property-binding test.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: KoreProperties.hierarchy + thread into KoreAgentFactory/KoreAutoConfiguration</name>
  <read_first>
    - kore-spring/.../KoreProperties.kt (BudgetProperties template lines 112-115; root ctor 20-28; intentionally-extensible KDoc 108-111)
    - kore-spring/.../KoreAgentFactory.kt (full file — port pre-wire before block(), lines 36-45)
    - kore-spring/.../KoreAutoConfiguration.kt (koreAgentFactory bean lines 75-81; inMemoryBudgetEnforcer reading properties.budget.defaultMaxTokens lines 62-65)
    - the Plan 03 AgentBuilder.maxDepth(n) method; 07-PATTERNS.md "KoreProperties.kt" + "KoreAutoConfiguration.kt / KoreAgentFactory.kt"; RESEARCH "Spring property surface" code example
  </read_first>
  <action>
    In KoreProperties.kt, add to the root data-class constructor (after eventBus, ~line 27):
    a hierarchy: HierarchyProperties = HierarchyProperties() field, and add a nested
    data class HierarchyProperties(val maxDepth: Int = 5) with KDoc:
    "Hierarchical-agent spawn ceiling. kore.hierarchy.max-depth, default 5 (HIER-03)."
    Mirror the BudgetProperties shape exactly (D-08). Keep the shape extensible.

    In KoreAgentFactory.kt, thread the maxDepth value into every built agent. Add a
    maxDepth: Int constructor parameter (defaulted to 5 to preserve existing callers) and,
    inside agent(...), call maxDepth(this@KoreAgentFactory.maxDepth) alongside the existing
    eventBus()/auditLog()/skillRegistry() pre-wiring BEFORE block() — so a user
    maxDepth(n) inside block still overrides it (user overrides run after, winning, exactly
    like the existing port-override precedence documented in the factory KDoc). Update the
    factory KDoc to mention maxDepth pre-wiring.

    In KoreAutoConfiguration.kt, update the koreAgentFactory @Bean (lines 75-81) to pass
    properties.hierarchy.maxDepth into the KoreAgentFactory constructor (the bean method
    already receives or can receive KoreProperties — add a KoreProperties parameter the
    same way inMemoryBudgetEnforcer does). Keep @ConditionalOnMissingBean(KoreAgentFactory::class).
    No var; no !!.
  </action>
  <verify>
    <automated>./gradlew :kore-spring:compileKotlin</automated>
  </verify>
  <acceptance_criteria>
    KoreProperties exposes hierarchy.maxDepth (default 5); KoreAgentFactory applies
    maxDepth before block; KoreAutoConfiguration passes properties.hierarchy.maxDepth into
    the factory bean; kore-spring compiles.
  </acceptance_criteria>
  <done>
    grep confirms HierarchyProperties in KoreProperties.kt and maxDepth threading in
    KoreAgentFactory.kt; :kore-spring:compileKotlin succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: KoreHierarchyPropertiesTest (ApplicationContextRunner)</name>
  <read_first>
    - any existing kore-spring ApplicationContextRunner test (e.g. the budget auto-config matrix from Phase 6 — find under kore-spring/src/test) for the exact runner setup + assertion style
    - KoreProperties.kt + the Task 1 changes; 07-RESEARCH.md Validation Architecture (Spring Boot @ConfigurationProperties)
  </read_first>
  <action>
    Create KoreHierarchyPropertiesTest.kt in kore-spring test package using
    ApplicationContextRunner (mirror the existing Phase 6 budget auto-config test shape —
    same withUserConfiguration / withPropertyValues / hasBean patterns; do NOT instantiate
    LLM backends that open sockets). Tests:
    - default: with no kore.hierarchy.* property, the bound KoreProperties.hierarchy.maxDepth
      == 5 (assert via getBean(KoreProperties::class) or @EnableConfigurationProperties).
    - override: withPropertyValues("kore.hierarchy.max-depth=9") binds maxDepth == 9.
    - factory threading: assert a KoreAgentFactory bean is present and (if cheaply
      observable without constructing a backend) that it carries the configured maxDepth —
      otherwise assert the property value reaches the factory via the bean wiring (a
      definition-level assertion, not a live agent run, to avoid backend construction).
    Use Kotest shouldBe assertions. @Tag the class only if the repo convention requires it
    (these are fast context tests, NOT @Tag("integration")).
  </action>
  <verify>
    <automated>./gradlew :kore-spring:test --tests "*KoreHierarchyPropertiesTest*"</automated>
  </verify>
  <acceptance_criteria>
    The test proves default maxDepth 5, override binding to 9, and that the value reaches
    KoreAgentFactory — without opening any network socket (definition-level assertions).
  </acceptance_criteria>
  <done>:kore-spring:test passes the new KoreHierarchyPropertiesTest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application.yml → KoreProperties | operator-supplied kore.hierarchy.max-depth binds into the depth ceiling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Denial of Service | `kore.hierarchy.max-depth` config | mitigate | The property is the operator's knob for the recursion ceiling enforced in AgentTool (Plan 03). It defaults to 5; an absent/blank config keeps the safe default (criterion #5). Setting it is a deliberate operator action, not attacker-controlled. |
| T-7-08 | Elevation of Privilege | property override precedence | accept | A user maxDepth(n) in the agent block overrides the Spring default by design (mirrors existing port-override precedence). Both surfaces are developer-controlled; no untrusted caller participates. |
| T-7-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero external packages added (RESEARCH Package Legitimacy Audit: Not applicable). |

No `high`-severity unmitigated threats — planning not blocked.
</threat_model>

<verification>
- `./gradlew :kore-spring:compileKotlin` green.
- `./gradlew :kore-spring:test` green including KoreHierarchyPropertiesTest.
- `./gradlew :kore-spring:lintKotlin` passes.
</verification>

<success_criteria>
- kore.hierarchy.max-depth binds (default 5), threads into KoreAgentFactory, user override wins.
- No behavioral change for apps that set no kore.hierarchy.* config (criterion #5).
</success_criteria>

<output>
Create `.planning/phases/07-hierarchical-agents/07-04-SUMMARY.md` when done.
</output>
