---
phase: 07-hierarchical-agents
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - kore-storage/src/test/kotlin/io/github/unityinflow/kore/storage/PostgresAuditLogAdapterTest.kt
autonomous: true
gap_closure: true
requirements: [HIER-02, HIER-03, HIER-04]
must_haves:
  truths:
    - "AgentLoop.run returns an AgentResult (never throws) when the injected AuditLog throws — proven by an executed unit test (already committed eb370c0; re-asserted green)."
    - "Concurrent parent runs sharing one AgentTool instance each receive their OWN parentRunId/depth on a real multi-threaded dispatcher — proven by an executed unit test (already committed eb370c0; re-asserted green)."
    - "PostgresAuditLogAdapter.recordAgentRun persists a non-UUID free-form agentId without throwing, deriving a deterministic UUID PK — proven by a NEW @Tag(\"integration\") test."
    - "A non-UUID child whose parentRunId is a non-UUID free-form parent id still correlates to the parent row (run-tree preserved under the deterministic UUID derivation) — proven by the NEW integration test."
  artifacts:
    - path: "kore-storage/src/test/kotlin/io/github/unityinflow/kore/storage/PostgresAuditLogAdapterTest.kt"
      provides: "Non-UUID agentId persistence + non-UUID parent/child correlation integration coverage (Gap 2 missing item #3)"
      contains: "nameUUIDFromBytes"
  key_links:
    - from: "PostgresAuditLogAdapterTest (non-UUID test)"
      to: "PostgresAuditLogAdapter.toStableUuid"
      via: "recordAgentRun with a non-UUID id, queried back via the same deterministic derivation"
      pattern: "nameUUIDFromBytes|toStableUuid"
---

<objective>
Close the single remaining gap from 07-VERIFICATION.md (Gap 2, `missing` item #3): the persistent
Postgres audit path has no test that submits a NON-UUID `agentId`. Every existing
`PostgresAuditLogAdapterTest` case uses `UUID.randomUUID()`, so the CR-02 crash path
(`UUID.fromString("parent-1")` -> `IllegalArgumentException` out of `run()`) had zero coverage even
when Testcontainers works.

IMPORTANT — most of Phase 07's gap work is ALREADY DONE and committed in `eb370c0`
("triage phase-07 BLOCKERs CR-01 + CR-02"), which landed ~8h AFTER the verifier ran at
2026-06-22T07:05Z and scored the PRE-triage code 3/5. Do NOT re-implement those fixes. Already in
the tree and committed:
  - CR-01 (Gap 1): `ChildLineage` CoroutineContext element replaces the shared `parentDepthCell`/
    `parentRunIdCell`; `ChildDispatchBinder.bind()` is now a pure function returning the element;
    `AgentLoop` installs it via `withContext(childLineage)`; `AgentTool.callTool` reads it from
    `coroutineContext[ChildLineage]`. The demanded multi-threaded concurrency test
    (`CR-01 concurrent binds on one shared AgentTool do not corrupt per-run lineage`, on
    `Dispatchers.Default`) is committed in `AgentToolTest`.
  - CR-02 (Gap 2): `AgentLoop.run`'s success/error `.also` audit write is wrapped in `runCatching`
    (no-throw invariant restored); `PostgresAuditLogAdapter` uses `String.toStableUuid()`
    (verbatim UUID parse, else deterministic `UUID.nameUUIDFromBytes`) instead of bare
    `UUID.fromString`. The demanded no-throw unit test
    (`CR-02 run returns an AgentResult and does not throw when AuditLog throws`) is committed in
    `AgentLoopTest`.
  - WR-01 (budget inheritance): `AgentBuilder.child` now passes `budgetEnforcer = budgetEnforcer`
    into the child builder — already inherited.

This plan therefore does TWO things only: (1) add the one missing non-UUID integration test, and
(2) re-assert the already-committed unit tests are green so re-verification can confirm 5/5 without
duplicating committed work.

Purpose: make HIER-02/HIER-03/HIER-04 contract-complete on the persistent path so re-verification
scores 5/5 (criteria #2 and #4 currently PARTIAL).
Output: one new `@Tag("integration")` test method in `PostgresAuditLogAdapterTest.kt`; a green
re-run of the kore-core suite proving the committed concurrency + no-throw tests pass.

Out of scope (deliberate, do NOT add):
  - CR-03 (replace hand-rolled `AgentTool.extractInput` with `kotlinx.serialization`): in kore-core,
    `kotlinx-serialization-json` is `compileOnly` by deliberate published-library design (zero hard
    runtime deps; the host supplies the JSON runtime). `Json.decodeFromString` at runtime would
    break that contract. It is a REVIEW warning, NOT a verification criterion and NOT in either
    gap's `missing`. Leave `extractInput` as-is; this is a recorded deferral, not an omission.
  - The Ryuk/Testcontainers env timeout is environment-only (human-verification item per
    07-VERIFICATION.md), NOT a code gap. The canonical integration gate is CI (arc-runner-unityinflow).
    Do NOT create work to "fix" Ryuk.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/07-hierarchical-agents/07-VERIFICATION.md
@.planning/phases/07-hierarchical-agents/07-REVIEW.md
@kore-storage/src/main/kotlin/io/github/unityinflow/kore/storage/PostgresAuditLogAdapter.kt
@kore-storage/src/test/kotlin/io/github/unityinflow/kore/storage/PostgresAuditLogAdapterTest.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add non-UUID agentId + non-UUID parent/child correlation integration test</name>
  <files>kore-storage/src/test/kotlin/io/github/unityinflow/kore/storage/PostgresAuditLogAdapterTest.kt</files>
  <read_first>
    - kore-storage/src/test/kotlin/io/github/unityinflow/kore/storage/PostgresAuditLogAdapterTest.kt
      (mirror its exact shape: `@Tag("integration")`, `@Testcontainers`, the shared `companion object`
      `postgres` container + `adapter`, `runTest { ... }`, and the `postgres.createConnection("").use { conn -> ... }`
      JDBC-readback idiom used by `recordAgentRun inserts a row readable via JDBC` and
      `child run persists parent_run_id equal to parent id`).
    - kore-storage/src/main/kotlin/io/github/unityinflow/kore/storage/PostgresAuditLogAdapter.kt
      (the file-private `String.toStableUuid()` at the bottom: a valid UUID string parses verbatim,
      any other string maps to `UUID.nameUUIDFromBytes(toByteArray(Charsets.UTF_8))` — the test must
      query rows by the SAME derivation, because the stored PK is the derived UUID, not the raw string).
  </read_first>
  <action>
    Add ONE new `@Test` method to the existing `PostgresAuditLogAdapterTest` class (do not create a
    new class; reuse the shared container/adapter). Name it
    `recordAgentRun persists a non-UUID agentId and preserves non-UUID parent-child correlation (CR-02)`.

    Within `runTest`:
    1. Use FREE-FORM, non-UUID ids exactly like the in-memory tests do: parent agentId `"parent-1"`,
       child agentId `"child-1"`, with the child's `AgentTask(parentRunId = "parent-1")`. Build
       `AgentTask` instances with non-UUID `id` values too (e.g. `id = "task-parent-1"`), since
       `task.id` is the free-form String the verifier called out.
    2. Record the parent run then the child run via `adapter.recordAgentRun(...)`. The call MUST NOT
       throw — that is the core regression assertion (pre-`toStableUuid`, `UUID.fromString("parent-1")`
       threw). No-exception is implicit (the test body fails if it throws); ALSO add a comment at each
       call site referencing CR-02 / the run-NEVER-throws invariant.
    3. Compute the expected stored PKs in the test the SAME way the adapter does — replicate the
       derivation inline as a small test-local helper (a `String.() -> UUID` mirroring
       `runCatching { UUID.fromString(this) }.getOrElse { UUID.nameUUIDFromBytes(toByteArray(Charsets.UTF_8)) }`).
       Do NOT import the private adapter helper; recompute it so the test independently proves the
       contract.
    4. Read back via JDBC (`postgres.createConnection("").use { conn -> ... }`):
       - Assert exactly ONE `agent_runs` row exists for the child's derived UUID
         (`SELECT count(*) FROM agent_runs WHERE id = ?::uuid`, bind the derived child UUID as text ->
         count `shouldBe 1`).
       - Assert that child row's `parent_run_id::text` equals the parent's DERIVED UUID (the
         `toStableUuid("parent-1")` value), proving the run-tree correlation survives the non-UUID ->
         deterministic-UUID mapping (HIER-04 criterion #4 on the persistent path with non-UUID ids).
    Keep the test `@Tag("integration")` by class-level inheritance (the class is already tagged).
    Follow CLAUDE.md Kotlin conventions: `val` only, no `!!`, Kotest `shouldBe` matchers, no
    `Thread.sleep`. ktlint-clean (import ordering, no wildcard imports).
  </action>
  <verify>
    <automated>./gradlew :kore-storage:lintKotlin :kore-storage:compileTestKotlin 2>&1 | tail -5</automated>
    <human-check>Run `./gradlew :kore-storage:integrationTest --tests "*PostgresAuditLogAdapterTest*"` on a host where Testcontainers/Ryuk starts (CI arc-runner-unityinflow, or local Docker with the reaper enabled). Expected: the new non-UUID test is GREEN — recordAgentRun did not throw on a non-UUID id, the child row exists under its derived UUID, and its parent_run_id equals toStableUuid("parent-1"). The verifier could not start Ryuk locally (env constraint), so execution is gated on CI/Docker.</human-check>
  </verify>
  <acceptance_criteria>
    - A new `@Test` named for CR-02 / non-UUID exists in `PostgresAuditLogAdapterTest`.
    - The test submits a non-UUID `agentId` AND a non-UUID `parentRunId` (free-form strings like "parent-1").
    - The test compiles and is ktlint-clean (`compileTestKotlin` + `lintKotlin` BUILD SUCCESSFUL).
    - The test asserts the child row is found by its DERIVED UUID and its `parent_run_id` equals the parent's DERIVED UUID.
    - No production code is modified (the `toStableUuid` fix is already committed in eb370c0).
  </acceptance_criteria>
  <done>The persistent path has executed-or-CI-runnable coverage proving a non-UUID agentId neither crashes recordAgentRun nor breaks parent->child run-tree correlation — closing Gap 2 missing item #3.</done>
</task>

<task type="auto">
  <name>Task 2: Re-assert committed CR-01 concurrency + CR-02 no-throw unit tests are green</name>
  <files>kore-core/src/test/kotlin/io/github/unityinflow/kore/core/AgentToolTest.kt, kore-core/src/test/kotlin/io/github/unityinflow/kore/core/AgentLoopTest.kt</files>
  <read_first>
    - kore-core/src/test/kotlin/io/github/unityinflow/kore/core/AgentToolTest.kt
      (confirm the committed test `CR-01 concurrent binds on one shared AgentTool do not corrupt
      per-run lineage` exists and dispatches on `Dispatchers.Default`, NOT a single-threaded test
      dispatcher — this is the verifier's demanded multi-coroutine real-dispatcher test for Gap 1).
    - kore-core/src/test/kotlin/io/github/unityinflow/kore/core/AgentLoopTest.kt
      (confirm the committed test `CR-02 run returns an AgentResult and does not throw when AuditLog
      throws` exists and uses an `AuditLog` whose `recordAgentRun` throws — Gap 2 no-throw unit test).
    - kore-core/src/main/kotlin/io/github/unityinflow/kore/core/port/AgentTool.kt and
      kore-core/src/main/kotlin/io/github/unityinflow/kore/core/port/ChildLineage.kt
      (confirm the shared `parentDepthCell`/`parentRunIdCell` are GONE and lineage flows through
      `coroutineContext[ChildLineage]`).
  </read_first>
  <action>
    This is a verification-only task — do NOT add or rewrite these tests (they are already committed
    in eb370c0). Run the kore-core test suite and confirm both gap-proving unit tests execute and
    pass, alongside the pre-existing HIER-01/02/03/04 cases and the binary-compat suite (criterion #5).

    Concretely:
    1. Grep-confirm the two committed test names are present (fail loudly if either is missing — that
       would mean the triage commit is not in the working tree and this whole gap-closure premise is
       wrong; surface that rather than silently passing).
    2. Grep-confirm `AgentTool.kt` no longer contains `parentDepthCell` or `parentRunIdCell` (CR-01
       shared-state removal), and that `coroutineContext[ChildLineage]` is read in `callTool`.
    3. Run the kore-core suite (full, rerun to defeat caching) and confirm 0 failures, including both
       named tests.
    If any of (1)-(3) fail, STOP and report — do not attempt to recreate the committed fixes; that
    would duplicate work and risks divergence.
  </action>
  <verify>
    <automated>grep -q 'CR-01 concurrent binds on one shared AgentTool' kore-core/src/test/kotlin/io/github/unityinflow/kore/core/AgentToolTest.kt && grep -q 'CR-02 run returns an AgentResult and does not throw' kore-core/src/test/kotlin/io/github/unityinflow/kore/core/AgentLoopTest.kt && ! grep -qE 'parentDepthCell|parentRunIdCell' kore-core/src/main/kotlin/io/github/unityinflow/kore/core/port/AgentTool.kt && echo OK</automated>
    <automated>./gradlew :kore-core:test --rerun-tasks 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Both committed gap-proving test names are present (grep gate passes).
    - `AgentTool.kt` contains no `parentDepthCell`/`parentRunIdCell` shared-state cells (CR-01 removed).
    - `./gradlew :kore-core:test --rerun-tasks` reports 0 failures, including both named tests and the unchanged binary-compat suite (criterion #5).
    - No test or production code is added/modified by this task (verification-only).
  </acceptance_criteria>
  <done>The CR-01 concurrency and CR-02 no-throw guarantees are demonstrated by executed, green unit tests on a real multi-threaded dispatcher (Gap 1) and a throwing-AuditLog (Gap 2) — re-verifiable without re-doing committed work.</done>
</task>

</tasks>

<verification>
After both tasks:
- `./gradlew :kore-core:test --rerun-tasks` — 0 failures (CR-01 concurrency test + CR-02 no-throw test + all HIER-01..04 + binary-compat suite green).
- `./gradlew :kore-storage:lintKotlin :kore-storage:compileTestKotlin` — BUILD SUCCESSFUL (new non-UUID integration test compiles + ktlint-clean).
- Persistent-path integration run on CI/Docker (`./gradlew :kore-storage:integrationTest --tests "*PostgresAuditLogAdapterTest*"`) — new non-UUID test green (human/CI-gated; Ryuk env constraint).

These close the two PARTIAL criteria so re-verification can score 5/5:
- Criterion #2 (HIER-02 structured concurrency under concurrent runs) — proven by the committed CR-01 multi-threaded test (re-asserted green).
- Criterion #4 (HIER-04 traceable run trees on the persistent path) — proven by the committed CR-02 no-throw unit test PLUS the new non-UUID integration test (correlation survives non-UUID ids).
- Criterion #3 (HIER-03 depth ceiling) — no longer at risk now that depth is read per-coroutine from ChildLineage (the racy shared cell that could corrupt depth is gone).
</verification>

<success_criteria>
- New `07-05-PLAN.md` exists with `gap_closure: true`.
- The one outstanding `missing` item (non-UUID integration test) has a concrete task + a test proving it.
- Already-committed CR-01/CR-02 fixes are re-asserted (not duplicated) so re-verification confirms 5/5.
- HIER-02/HIER-03/HIER-04 re-asserted in frontmatter `requirements`.
- No production code re-implemented (eb370c0 already landed the fixes).
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces (07-05 gap closure)

New / changed symbols:
- `PostgresAuditLogAdapterTest.recordAgentRun persists a non-UUID agentId and preserves non-UUID parent-child correlation (CR-02)` — NEW `@Tag("integration")` test method (the only new artifact; closes Gap 2 missing item #3).

Already committed in eb370c0 (re-asserted, NOT produced here):
- `port/ChildLineage.kt` — per-coroutine lineage CoroutineContext element (CR-01).
- `ChildDispatchBinder.bind(): ChildLineage` — pure binding contract (CR-01).
- `AgentTool.callTool` — reads `coroutineContext[ChildLineage]`; no shared cells (CR-01).
- `AgentLoop.run` — `runCatching`-guarded `.also` audit write (CR-02 no-throw).
- `PostgresAuditLogAdapter` `String.toStableUuid()` — deterministic UUID derivation (CR-02 non-UUID).
- `AgentToolTest.CR-01 concurrent binds ... do not corrupt per-run lineage` — multi-threaded test (Gap 1).
- `AgentLoopTest.CR-02 run returns an AgentResult and does not throw when AuditLog throws` — no-throw unit test (Gap 2).
- `AgentBuilder.child` — inherits `budgetEnforcer` (WR-01).

Deliberately NOT changed:
- `AgentTool.extractInput` (CR-03) — kotlinx.serialization is `compileOnly` in kore-core by design; runtime decode would break the zero-runtime-dep contract. REVIEW warning only, not a verification criterion. Recorded deferral.
</artifacts_produced>

<output>
Create `.planning/phases/07-hierarchical-agents/07-05-SUMMARY.md` when done.
</output>
