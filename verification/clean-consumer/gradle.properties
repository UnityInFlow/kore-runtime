# Default kore version so this standalone build is runnable without -P locally.
# The verify-published.sh script and verify-published.yml workflow ALWAYS override this with the
# real version via -PkoreVersion=<x>; this default just keeps the build configurable.
koreVersion=0.1.0
